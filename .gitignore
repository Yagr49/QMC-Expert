# Unrelated vendored codebase (COMA generative model, 737 MB)
COMA_new/

# Large benchmark archives (>20 MB, not used by this paper)
data/*.tar.gz
*.tar.gz

# Office lock files
~$*

# OS / tooling
.DS_Store
catboost_info/
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.env

# Secrets
*.key
secrets.*
.git_broken_backup_mathcom/

# fine-tuning run artifacts (transient)
reproducibility/finetune/progress.json*
reproducibility/finetune/train.log
reproducibility/finetune/.server.log
reproducibility/finetune/data/*.npy
reproducibility/finetune/progress_direct.json*
reproducibility/finetune/direct.log
reproducibility/finetune/chain.log
reproducibility/finetune/checkpoints/
reproducibility/finetune/__pycache__/
